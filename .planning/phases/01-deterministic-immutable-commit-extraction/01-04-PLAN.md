---
phase: 01-deterministic-immutable-commit-extraction
plan: 04
name: Complete structural projection and coarse fallback
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - integrations/vibery-station/lib/station-projector.mjs
  - integrations/vibery-station/test/station-projector.test.mjs
autonomous: true
requirements:
  - MAP-02
  - MAP-03
must_haves:
  truths:
    - A supported root package or one-to-five first-path-segment groups yields a complete, disjoint, exhaustive structural map.
    - Cross-room relations aggregate only exact declared internal package dependencies and never create same-room self-loops or runtime claims.
    - Every integrity-valid unsupported shape yields exactly one evidence-bound coarse room and zero relations with explicit sorted reason codes.
  artifacts:
    - integrations/vibery-station/lib/station-projector.mjs
    - integrations/vibery-station/test/station-projector.test.mjs
  key_links:
    - station-projector.mjs is a pure function of validated evidence value plus exact evidence bytes/hash.
    - Room/relation/snapshot IDs come only from identity.mjs and map bytes come only from canonical-json.mjs.
---

<objective>
Implement the only allowed Phase 1 topology policy: complete structural workspace grouping or one explicit coarse project room.

Purpose: Convert proven manifest structure into useful Station primitives without unsupported service, ownership, runtime, or semantic claims.
Output: A pure deterministic projector and exhaustive policy tests.
</objective>

<execution_context>
@.planning/phases/01-deterministic-immutable-commit-extraction/01-RESEARCH.md
@integrations/vibery-station/lib/node-workspace-evidence.mjs
@integrations/vibery-station/lib/identity.mjs
@integrations/vibery-station/lib/contracts.mjs
</execution_context>

<context>
The room policy is locked: a non-workspace root package is `root-package`; workspace package roots are grouped by their first path segment; detailed output is permitted only for a complete 1–5-group decomposition. Labels expose only the structural key (or deterministic repository basename for coarse display) and must not claim frontend/backend/service/runtime/ownership semantics.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project complete detailed rooms and exact aggregated relations</name>
  <files>integrations/vibery-station/lib/station-projector.mjs, integrations/vibery-station/test/station-projector.test.mjs</files>
  <action>
Implement `projectStationMap(validatedEvidence, evidenceBytes)` as a pure function. Verify the supplied evidence-byte SHA-256 before use. For a root non-workspace package, create exactly one high-confidence component room with structural key `root-package`. For supported workspaces, group every package root by its first POSIX path segment, require 1–5 non-empty groups, and create one room per group with sorted exhaustive `package_roots` and unique sorted manifest evidence IDs. Derive room IDs from structural keys; labels are the exact structural segment and do not participate in identity.

Resolve internal packages only by exact selected package `name`. For each declaration crossing groups, aggregate into one directed `(from_room_id,to_room_id)` relation, union/sort exact declaration scopes and declaring manifest evidence IDs, and derive its ID from the endpoint pair. Keep same-room declarations in evidence but emit no self-loop. Sort rooms and relations by ID, compute snapshot ID from project/revision/evidence hash/profile, validate `station-map/v1`, then emit canonical bytes.
  </action>
  <verify>
`node --test integrations/vibery-station/test/station-projector.test.mjs` proves root-only and each 1–5 group count, complete/disjoint package membership, stable ordering under evidence permutations, exact relation direction/scope/evidence aggregation, no same-room loop, and unchanged room/relation IDs after display-name-only changes.</verify>
  <done>Every detailed map is a complete structural projection of all supported packages with exact declaration-backed relation directions.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse every supported-but-underdetermined shape to coarse truth</name>
  <files>integrations/vibery-station/lib/station-projector.mjs, integrations/vibery-station/test/station-projector.test.mjs</files>
  <action>
When validated evidence carries any approved `station-fallback/*` reason or grouping produces zero/more-than-five groups, emit exactly one room: `kind: coarse-project`, `structural_key: project-root`, `confidence: coarse`, no package-root detail, and all available selected-manifest evidence IDs sorted. Set `fallback.used: true`, include one or more unique sorted versioned reason codes, and emit zero relations. Bind the room through project ID and the map through exact evidence hash/snapshot ID. Use only the root package name when valid or canonical repository basename as deterministic display label; neither affects identity.

Reject rather than project if evidence fails its strict contract, project/repository identity is inconsistent, evidence bytes do not match the value, or an unknown fallback code appears. Those are integrity/gate failures, not fallback. Ensure no code path selects a subset of five groups/packages and no coarse result carries detailed confidence, detailed membership, or relations.
  </action>
  <verify>
`node --test integrations/vibery-station/test/station-projector.test.mjs` table-tests every reason emitted by the evidence builder (missing/invalid/oversized/non-regular manifest, unsupported workspace shape, zero match, encoding/path collision, budget, ambiguous package name, and room count) and asserts one coarse room, zero relations, sorted reasons, valid evidence binding, and no partial detail. Invalid/tampered evidence must throw a typed hard diagnostic.</verify>
  <done>Unsupported but trustworthy repositories receive a truthful one-room map, while any integrity defect fails instead of hiding behind fallback.</done>
</task>

</tasks>

<verification>
- `node --test integrations/vibery-station/test/station-projector.test.mjs`
- Search projected fixture labels/kinds and assert none introduce `frontend`, `backend`, `service`, `database`, `owner`, `calls`, or causal/runtime terminology.
- Property assertions prove detailed room package-root union equals the full evidence package-root set and pairwise intersections are empty.
</verification>

<success_criteria>
- Detailed output exists only for complete root/1–5 structural groups.
- Relations exactly equal all and only cross-room declared internal dependency directions.
- Every approved unsupported shape is explicit one-room coarse output with no partial topology.
- Map value and bytes validate, sort, and hash deterministically.
</success_criteria>

<non_goals>
- No semantic room names/types, top-five truncation, LLM input, import/runtime graph, lockfile resolution, layout, rendering, Viewer/Unity projection, or publication.
</non_goals>

<output>
After completion, create `01-04-SUMMARY.md` with detailed/fallback fixture matrix and exact relation policy evidence.
</output>
