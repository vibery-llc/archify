---
phase: 01-deterministic-immutable-commit-extraction
plan: 05
name: Independent fail-closed Station gate
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - integrations/vibery-station/lib/station-gate.mjs
  - integrations/vibery-station/test/station-gate.test.mjs
autonomous: true
requirements:
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - The gate reparses exact evidence/map bytes and independently reconstructs the complete expected evidence ledger and map rather than trusting either builder output.
    - Canonical byte equality, complete detailed topology, coarse fallback invariants, and every reference/identity/digest/count/order rule are publication prerequisites.
    - Any mismatch is a typed hard failure and cannot be downgraded to fallback or reach publication.
  artifacts:
    - integrations/vibery-station/lib/station-gate.mjs
    - integrations/vibery-station/test/station-gate.test.mjs
  key_links:
    - station-gate.mjs imports only contracts/canonical/identity primitives plus the verified reader session; it deliberately does not import or call node-workspace-evidence.mjs or station-projector.mjs.
    - The extraction orchestrator added later can publish only a successful gate result containing frozen validated bytes and recomputed facts.
---

<objective>
Add an independent evidence-to-map authority that admits only the one mechanically valid map for the supplied canonical evidence bytes.

Purpose: Prevent a projector bug, mutation, or authored claim from becoming Station topology authority.
Output: A fail-closed gate API and table-driven mutation tests.
</objective>

<execution_context>
@.planning/phases/01-deterministic-immutable-commit-extraction/01-RESEARCH.md
@integrations/vibery-station/lib/contracts.mjs
@integrations/vibery-station/lib/canonical-json.mjs
@integrations/vibery-station/lib/identity.mjs
@integrations/vibery-station/lib/station-projector.mjs
</execution_context>

<context>
Independence is a code boundary, not just a second schema check. The gate may share canonical serialization, strict contract validators, hash functions, and version constants. It must not call the projector or trust its room/relation/member/reference/count/fallback calculations. Intentional duplication of the small grouping/relation policy is required so gate and projector cannot share the same policy bug by construction.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reparse and independently rederive the only valid evidence/map pair</name>
  <files>integrations/vibery-station/lib/station-gate.mjs, integrations/vibery-station/test/station-gate.test.mjs</files>
  <action>
Implement a gate that accepts Buffers for evidence and map plus the already verified immutable reader session. Decode both with fatal UTF-8, require exactly one final LF and no other noncanonical representation, parse JSON, strict-validate supported schema/profile versions and unknown fields, canonical-reserialize, and byte-compare each input. Reconfirm the session's identity-derived Station canonical URL, repository identity, revision, tree OID, object format, fixed budgets, and complete tree inventory.

Without importing or calling `node-workspace-evidence.mjs`, independently repeat the bounded workspace grammar over that complete inventory: select the root/workspace manifests, run exact OID size probes, classify policy count/per-file/aggregate size excess, reread eligible blobs, decode/parse them, reconstruct workspace membership, package fields, exact workspace-pattern provenance, declaration names/scopes, selected-file accounting, approved fallback causes, every file digest/byte count/evidence ID, and every sorted count/reference invariant. Canonicalize this independently expected `station-evidence/v1` value and byte-compare it to the supplied evidence. Missing objects, malformed probes, probe/read disagreement, unknown or false fallback reasons, selected-file omission/invention, or any semantic mismatch are hard failures; a valid detailed repository cannot be authored as coarse fallback.

Then, without importing or calling `station-projector.mjs`, independently rebuild the only valid root/first-segment grouping and cross-room declaration aggregation from the reconstructed evidence. For detailed mode require 1–5 complete, disjoint, exhaustive rooms; exact structural keys/IDs/labels/confidence/package roots/evidence sets; exact relation endpoint/direction/scope/evidence/ID set; no omitted allowed relation; and no invented/self-loop relation. For fallback require the independently proven approved reasons, exactly one `coarse-project` room, project-root key, coarse confidence, no detailed membership, and zero relations. Recompute project/snapshot identities, canonicalize the independently expected map, and compare bytes to the supplied map.
  </action>
  <verify>
`node --test integrations/vibery-station/test/station-gate.test.mjs` accepts builder/projector output, rejects noncanonical but parseable JSON, and rejects isolated mutations to schema/profile, canonical repository URL, revision/tree/evidence hash, every ID class, selected-file set, blob digest/size, workspace declaration/pattern/membership, package name/private/root, dependency name/scope, ordering, references, room membership/evidence sets, relation endpoints/scopes, omitted/invented relation, counts, confidence, and false/omitted fallback causes. Boundary tests prove exact policy-size excess is independently rederived as fallback while object/probe inconsistency is hard failure.</verify>
  <done>The gate accepts only byte-canonical evidence and map bytes independently reconstructed from the verified Git inventory and blobs.</done>
</task>

<task type="auto">
  <name>Task 2: Expose stable hard diagnostics and a publication-safe result</name>
  <files>integrations/vibery-station/lib/station-gate.mjs, integrations/vibery-station/test/station-gate.test.mjs</files>
  <action>
Classify failures into stable `station-gate/schema-invalid`, `station-gate/evidence-reference-missing`, `station-gate/evidence-identity-mismatch`, `station-gate/topology-identity-mismatch`, `station-gate/unsupported-claim`, and canonical/order mismatch diagnostics. Include bounded logical subjects/evidence only—never absolute paths, raw manifest bytes, credentials, stacks, or volatile process data.

Return a frozen success object containing the exact accepted evidence/map Buffers plus recomputed repository, project/snapshot IDs, mode, room/relation counts, and exact hashes needed to build the receipt. Do not return success alongside warnings/errors. Unknown fallback reasons, malformed bytes, projector exceptions, or any mutation are hard failures and must not synthesize a coarse result.
  </action>
  <verify>
The focused gate suite asserts one stable diagnostic envelope per mutation class, no fallback result on any gate error, immutable success buffers/metadata, and no absolute temp path or secret-bearing URL in serialized diagnostics.</verify>
  <done>Callers receive either one frozen, independently validated publication payload or one typed hard failure, never a partially trusted result.</done>
</task>

</tasks>

<verification>
- `node --test integrations/vibery-station/test/station-gate.test.mjs`
- `rg "node-workspace-evidence|buildStationEvidence|station-projector|projectStationMap" integrations/vibery-station/lib/station-gate.mjs` returns no matches.
- Mutation coverage includes unknown fields, all derived identity classes, all references, ordering/count/completeness, detailed relations, and every coarse invariant.
</verification>

<success_criteria>
- Gate validation begins from exact bytes and the verified complete Git inventory, then ends with exact-byte comparison to independently reconstructed evidence and map values.
- Every selected-file, workspace, package, declaration, fallback, identity, topology, count, order, and reference claim in both artifacts is recomputed or structurally accounted for.
- No integrity/gate failure can become coarse fallback.
- Only gate success provides data consumable by publication.
</success_criteria>

<non_goals>
- No new Git command surface beyond bounded exact-OID rereads through the verified reader session, no projector reuse, output path handling, filesystem publication, receipt writing, renderer/browser invocation, or arbitrary user-authored topology validation.
</non_goals>

<output>
After completion, create `01-05-SUMMARY.md` with the independent recomputation list and mutation-to-diagnostic matrix.
</output>
