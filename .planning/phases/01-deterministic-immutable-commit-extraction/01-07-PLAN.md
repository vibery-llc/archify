---
phase: 01-deterministic-immutable-commit-extraction
plan: 07
name: Clone-independent determinism and Vibery-shaped acceptance
type: execute
wave: 7
depends_on:
  - 01-06
files_modified:
  - integrations/vibery-station/test/helpers/station-fixtures.mjs
  - integrations/vibery-station/test/determinism-acceptance.test.mjs
  - integrations/vibery-station/test/vibery-shape.test.mjs
autonomous: true
requirements:
  - TEST-01
  - TEST-03
must_haves:
  truths:
    - The same repository identity and commit emits byte-identical evidence, map, and receipt across equivalent HTTPS/SSH/SCP remote spellings, clone paths, checkout branches, dirty worktrees, locale/timezone, and repeated runs.
    - A README-only commit changes revision-bound snapshot identity while preserving project, room, and relation identities.
    - A twelve-workspace Vibery-shaped fixture yields four structural rooms and exactly three expected cross-room directions with no product-specific implementation branch or LLM.
  artifacts:
    - integrations/vibery-station/test/determinism-acceptance.test.mjs
    - integrations/vibery-station/test/vibery-shape.test.mjs
    - integrations/vibery-station/test/helpers/station-fixtures.mjs
  key_links:
    - Acceptance tests exercise the real integration CLI and read the three published files rather than only calling pure helpers.
    - Fixture helpers create temporary local Git repositories with synthetic origins and never invoke a remote or package manager.
---

<objective>
Prove the end-to-end happy-path contract is clone-independent, revision-aware, topology-stable, and correct for the required twelve-workspace shape.

Purpose: Establish exact-byte and visible structural acceptance evidence before the adversarial closure plan.
Output: Provider-free end-to-end deterministic and Vibery-shaped tests.
</objective>

<execution_context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-deterministic-immutable-commit-extraction/01-RESEARCH.md
@.planning/codebase/TESTING.md
@integrations/vibery-station/bin/station-map.mjs
@integrations/vibery-station/test/helpers/git-fixture.mjs
</execution_context>

<context>
Use only `node:test`, `node:assert/strict`, temporary directories, local Git commands, and the actual integration CLI. No provider, network listener/client, browser, renderer, package install, or external test framework is allowed. A Vibery-shaped fixture is acceptance data, not a license for repository-name/path special cases in production code.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove exact bytes across clones and mutable checkout state</name>
  <files>integrations/vibery-station/test/helpers/station-fixtures.mjs, integrations/vibery-station/test/determinism-acceptance.test.mjs</files>
  <action>
Build deterministic local Git fixtures and create local clones/copies containing the same object graph and repository identity at different absolute paths. For a supported forge identity, vary authored/origin spelling across canonical HTTPS, SSH URI, and SCP forms while preserving identity; also vary checked-out branch/HEAD, cwd, dirty modifications/deletions, staged changes, untracked manifests, locale, timezone, and irrelevant environment ordering. Run the real CLI for the same full commit, resolve each bundle's `CURRENT` exactly once, and compare the three immutable-generation artifact files as Buffers. Assert exact equality, the one Station canonical repository URL, valid UTF-8, LF-only content, and exactly one trailing LF. Run twice in one clone and compare again.

Scan parsed successful artifacts recursively to reject either temp absolute path, branch names, timestamps, runtime/host/PID/Git version, and raw stderr. Before and after every run compare HEAD, index bytes/checksum, refs used by the fixture, and porcelain status to prove extraction does not mutate the repository.

Create a second commit changing only README bytes. Assert evidence/map/receipt revision-bound bytes and snapshot ID change, while project ID and the complete sets of room/relation IDs remain identical. Also assert package label/name changes do not alter structural room/relation identities unless the path group or relation endpoints actually change.
  </action>
  <verify>
`node --test integrations/vibery-station/test/determinism-acceptance.test.mjs` passes with exact Buffer comparisons and identity-set assertions, without skip conditions or network access.</verify>
  <done>The complete published set is demonstrably a function of canonical repository identity, exact commit objects, and contract version—not clone or checkout state.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the twelve-workspace four-room/three-direction projection</name>
  <files>integrations/vibery-station/test/helpers/station-fixtures.mjs, integrations/vibery-station/test/vibery-shape.test.mjs</files>
  <action>
Create a local fixture with twelve declared npm workspaces distributed across first segments `vibery-backend-new`, `vibery-ui-web`, `vibery-games`, and `packages`. Give packages neutral synthetic names and declarations that produce exactly backend-group -> packages, UI-group -> packages, and games-group -> packages, plus at least one same-`packages` declaration. Run the real CLI and assert four rooms with exhaustive sorted package roots, exactly those three directed relations, aggregated exact scopes/evidence IDs, and no same-room self-loop.

Assert implementation-source text has no fixture repository URL, twelve-package count, Vibery package names, or branch keyed to those four labels; labels arise solely from generic first-segment grouping. Assert no LLM/provider module or result participates. Include a generic renamed copy of the same shape and prove equivalent structural behavior under its different first-segment labels.
  </action>
  <verify>
`node --test integrations/vibery-station/test/vibery-shape.test.mjs` passes and prints no skipped/provider/browser tests; exact room and relation direction sets match the fixture contract.</verify>
  <done>The required current-shape projection is achieved by generic mechanical policy, not product-specific code.</done>
</task>

</tasks>

<verification>
- `node --test integrations/vibery-station/test/determinism-acceptance.test.mjs integrations/vibery-station/test/vibery-shape.test.mjs`
- Both tests invoke `integrations/vibery-station/bin/station-map.mjs` and verify on-disk artifacts.
- Test setup uses only local repositories and synthetic origins; no remote URL is contacted.
</verification>

<success_criteria>
- All three artifact bytes are equal across identity-equivalent remote spellings, clones, branches, dirty states, and repeats for one commit.
- README-only revision preserves durable topology IDs and changes snapshot identity.
- Twelve workspaces produce four complete structural rooms and exactly three required directions.
- No fixture-specific product logic or nonmechanical synthesis exists.
</success_criteria>

<non_goals>
- No adversarial/tamper/failure-injection matrix (Plan 08), live Vibery repository dependency, semantic architecture labels, UI/rendering, provider use, or performance benchmark.
</non_goals>

<output>
After completion, create `01-07-SUMMARY.md` with exact-byte comparison evidence, identity delta assertions, and the four-room/three-direction fixture result.
</output>
