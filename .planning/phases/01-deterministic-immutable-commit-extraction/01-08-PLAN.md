---
phase: 01-deterministic-immutable-commit-extraction
plan: 08
name: Adversarial closure and isolation proof
type: execute
wave: 8
depends_on:
  - 01-07
files_modified:
  - integrations/vibery-station/test/helpers/no-network.cjs
  - integrations/vibery-station/test/adversarial-extraction.test.mjs
  - integrations/vibery-station/test/gate-tamper.test.mjs
  - integrations/vibery-station/test/atomic-failure.test.mjs
  - integrations/vibery-station/test/isolation.test.mjs
autonomous: true
requirements:
  - BOUND-01
  - TEST-02
must_haves:
  truths:
    - Provider-free tests cover every hostile Git/path/manifest/resource/fallback class, every evidence/map gate-tamper class, and failures/interruption at every immutable-generation/current-pointer publication boundary.
    - Hard integrity failures never become coarse success and every recoverable failure preserves all three last-known-good files byte-for-byte.
    - The capability remains entirely under integrations/vibery-station and cannot load network/provider/browser/rendering/default Archify paths or change default Archify/package/archive behavior.
  artifacts:
    - integrations/vibery-station/test/adversarial-extraction.test.mjs
    - integrations/vibery-station/test/gate-tamper.test.mjs
    - integrations/vibery-station/test/atomic-failure.test.mjs
    - integrations/vibery-station/test/isolation.test.mjs
  key_links:
    - Adversarial tests execute the real CLI for repository-constructible cases and call the real orchestrator in-process with narrow injected seams only for malformed protocol, impossible-to-time filesystem failure, and rollback-failure cases.
    - Isolation checks combine source/import restrictions, blocked network primitives, unchanged core-tree evidence, and default CLI/archive baselines.
---

<objective>
Close Phase 1 with the full hostile-input, tamper, transaction-failure, no-network, and no-default-regression acceptance matrix.

Purpose: Demonstrate that the extractor fails closed under adversarial conditions and remains an opt-in isolated profile.
Output: Provider-free adversarial/failure-injection suites and explicit core-isolation evidence.
</objective>

<execution_context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-deterministic-immutable-commit-extraction/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@archify/test/repository-evidence.test.mjs
@archify/test/output-path.test.mjs
@archify/test/delivery-contract.test.mjs
@integrations/vibery-station/test/helpers/git-fixture.mjs
@integrations/vibery-station/test/helpers/station-fixtures.mjs
</execution_context>

<context>
This plan adds tests and any test-only blocker/shim needed to exercise already-planned seams; it does not add new product behavior. Use local temporary Git repositories and Node built-ins only. Stable expected classifications come from the hard-failure/fallback policy in research and Plans 02–06.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exercise the complete hostile Git, path, manifest, budget, and fallback matrix</name>
  <files>integrations/vibery-station/test/adversarial-extraction.test.mjs</files>
  <action>
Create table-driven real-CLI cases for active replacement refs (commit/tree/blob), branch/tag/abbreviated/non-commit revisions, missing commit/tree/blob objects with lazy fetch disabled, wrong/missing/credentialed origins with redaction, non-top-level roots, and unsupported object format. Exercise malformed/incomplete/over-budget tree protocol through the in-process orchestrator's injected reader seam, because production CLI subprocesses expose no injection channel. Assert stable hard diagnostics, nonzero status, unchanged authoritative `CURRENT`, and specifically no coarse downgrade.

Create integrity-valid unsupported cases for package manifest symlinks, gitlinks, binary/NUL content, invalid UTF-8, control-character/invalid paths, case and Unicode-normalization collisions, unsupported glob forms, missing/malformed/policy-oversized manifests, zero matches, duplicate package identities, >5 groups, 512-manifest and 1 MiB/8 MiB budget boundaries. Assert each documented fallback reason exactly, one coarse room, zero relations, and no partial package/room claims. Test boundary values on both sides of every count/byte limit; prove an exact size over policy is fallback while unavailable objects, malformed size probes, probe/read disagreement, or process-output overflow are hard failures. Include valid unusual spaces/tabs/newlines to prove NUL parsing does not split records incorrectly.
  </action>
  <verify>
`node --test integrations/vibery-station/test/adversarial-extraction.test.mjs` passes with an explicit case table covering every named requirement class and asserting hard-failure versus coarse-fallback category.</verify>
  <done>Every hostile evidence class is deterministic, typed, and either fails before publication or produces the one permitted coarse truth—never partial detail.</done>
</task>

<task type="auto">
  <name>Task 2: Mutate every gated claim and fail every transaction boundary</name>
  <files>integrations/vibery-station/test/gate-tamper.test.mjs, integrations/vibery-station/test/atomic-failure.test.mjs</files>
  <action>
From valid structural and coarse candidates, independently mutate: schema/profile/contract version; unknown property; repository URL/revision/tree/object format; selected-file omission/invention; manifest OID/SHA/bytes/path/root/reference; workspace declarations/patterns/membership; package name/private/root; dependency name/scope; project/evidence/room/relation/snapshot ID; ordering/duplication/count; room structural key/label/confidence/membership/evidence set; relation endpoint/direction/scope/evidence set; omitted valid relation; invented/self-loop relation; evidence hash; false/omitted/unknown fallback causes; sixth/duplicate/omitted room; and noncanonical UTF-8/JSON/newline bytes. Include coherently mutated evidence+map pairs and a valid detailed fixture falsely downgraded to an approved coarse reason. Assert a stable `station-gate/*` diagnostic and no publication call for every row.

Seed a complete immutable prior generation and `CURRENT`. Inject failure during generation file write/close/fsync, generation-directory rename/fsync, final path recheck, temporary pointer write/fsync, pointer rename, bundle-root fsync, pointer restoration, and recovery-material handling. Spawn child publishers terminated immediately before and after pointer rename, and coordinate concurrent readers that resolve `CURRENT` once while publication pauses. Assert every reader sees one complete old or new generation, never mixed bytes; a recoverable post-pointer fsync failure restores prior authority; no-prior-pointer failure exposes no partial generation as current; malformed/aliased/symlinked/conflicting paths fail closed; and a forced restoration failure returns only `station-output/commit-rollback-failed`, retains both immutable generations plus deterministic pointer recovery material, and never reports success. Phase 1 tests must not delete prior generations.
  </action>
  <verify>
`node --test integrations/vibery-station/test/gate-tamper.test.mjs integrations/vibery-station/test/atomic-failure.test.mjs` passes; test tables enumerate each evidence/map mutation and generation/pointer failure/interruption point by name so omissions are visible.</verify>
  <done>Every evidence and topology claim is independently reconstructed/tamper-rejected, and the single-pointer publication contract proves complete old-or-new authority or explicit retained recovery state.</done>
</task>

<task type="auto">
  <name>Task 3: Prove integration isolation, no network/provider path, and unchanged default Archify</name>
  <files>integrations/vibery-station/test/helpers/no-network.cjs, integrations/vibery-station/test/isolation.test.mjs</files>
  <action>
Add a test-only preload that throws on Node network client/listener/DNS/browser-opening primitives while allowing the fixed local Git child process. Run a successful and failing real extraction under it. Statistically inspect the integration runtime import graph and Git command allowlist: permit Node built-ins, integration-local modules, and only the existing repository-location/output-path helpers; reject update-check, brand capture, preview, visual-check, renderer, Viewer, provider/LLM SDK, telemetry, fetch/http/https/net/dns, shell command, package manager, and remote Git subcommands.

Assert the integration package has no dependencies, install hooks, host registration, or default-root mutation. Assert default `archify --help` has no Station route and run the existing no-install help/smoke command needed to prove it still responds normally. Verify the phase diff contains no source/generated/archive changes under `archify/`, `viewer/`, root `scripts/`, `examples/`, `generated/`, or `archify.zip`; preserve the mapped baseline SHA-256 `2657acf353d3fadfde472b2c799eb9a1fa3a3b344129066980b9b1c5935f0883` for `archify.zip` as execution evidence. Do not regenerate schemas, Viewer, examples, or archive.
  </action>
  <verify>
`node --test integrations/vibery-station/test/isolation.test.mjs` passes. Then run `git diff --exit-code d673e8300df60a5c8166abe78787fdc78f6b8000 -- archify viewer scripts examples generated archify.zip` and a Node SHA-256 assertion for `archify.zip`; both pass.</verify>
  <done>The Station extractor is demonstrably opt-in, local-only, dependency-free at runtime, and incapable of altering or invoking default Archify product surfaces.</done>
</task>

</tasks>

<verification>
- `node --test integrations/vibery-station/test/*.test.mjs`
- `git diff --exit-code d673e8300df60a5c8166abe78787fdc78f6b8000 -- archify viewer scripts examples generated archify.zip`
- `node -e "const fs=require('node:fs'),c=require('node:crypto'); const h=c.createHash('sha256').update(fs.readFileSync('archify.zip')).digest('hex'); if(h!=='2657acf353d3fadfde472b2c799eb9a1fa3a3b344129066980b9b1c5935f0883') throw Error(h)"`
- `node archify/bin/archify.mjs --help` still exposes only the preexisting default command surface.
</verification>

<success_criteria>
- Every adversarial/fallback/evidence-tamper/map-tamper/failure/interruption class named in TEST-02 has a provider-free executable assertion.
- Hard failures publish nothing; coarse fallback is independently proven and complete; publication selects only complete immutable generations through one atomic pointer.
- Runtime import/command instrumentation proves no network/provider/browser/rendering path.
- The repository diff and archive hash prove default Archify/core/generated/package behavior is untouched.
</success_criteria>

<non_goals>
- No new fallback semantics, production recovery journal, host API, package publication, renderer/Viewer/Unity behavior, remote fetch, browser evidence, dependency installation, performance load test, commit, or push.
</non_goals>

<output>
After completion, create `01-08-SUMMARY.md` with the adversarial matrix, evidence/map tamper matrix, generation/pointer failure and interruption matrix, isolation commands, retained-recovery procedure, and any residual filesystem limitation. Planning summaries/state are exempt from the integration-only implementation diff assertion. Update Phase 1 verification/state only if all eight plan summaries exist and all 16 requirements are satisfied.
</output>
